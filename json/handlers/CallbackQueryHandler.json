{
  "class_name": "CallbackQueryHandler",
  "summary": "Handler perantara untuk semua `callback_query` dari tombol inline.",
  "description": "Handler ini dipanggil oleh `UpdateHandler` setiap kali pengguna menekan tombol inline. Tugasnya hanya satu: meneruskan `callback_query` ke `CallbackRouter` untuk di-dispatch ke logika yang benar.",
  "dependencies": ["CallbackRouter"],
  "methods": [
    {
      "name": "__construct",
      "description": "Menginisialisasi handler dengan instance `App`.",
      "parameters": [{"name": "app", "type": "App", "description": "Objek aplikasi inti."}]
    },
    {
      "name": "handle",
      "description": "Meneruskan `callback_query` ke `CallbackRouter`.",
      "parameters": [{"name": "callback_query", "type": "array", "description": "Data callback query dari Telegram."}],
      "logic_flow": [
        "1. Memanggil `CallbackRouter->route(callback_query)` untuk pemrosesan lebih lanjut."
      ]
    }
  ]
}